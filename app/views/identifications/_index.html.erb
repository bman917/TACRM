

<ul id="foids">

    <% identifications.each do |identification| %>
      <%= render identification %>
    <% end %>

</ul>

<div class="toggler">

<%= form_for(@identification, remote: true) do |f| %>
  <% if @identification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@identification.errors.count, "error") %> prohibited this identification from being saved:</h2>

      <ul>
      <% @identification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    
      <%= f.select :foid_type, foid_types %>
      <%= f.text_field :foid %>
      <%= f.submit 'Save' %>
      <%= link_to 'Hide', '', id: "hide_foid_link"%>
      <%= f.hidden_field :profile_id %>
<% end %>

<%= add_link  %>

</div>